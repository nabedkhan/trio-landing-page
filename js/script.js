
$(document).ready(function(){
  
  // owl-carousel plugin activation
  $(".slider-list").owlCarousel({
	items: 1,
  	dots: true,
  	rtl: false,
  	autoplay:true,
  });

  // Masonry plugin activation
  $('.masonary').masonry({
  });

});